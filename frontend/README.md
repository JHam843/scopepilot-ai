# ScopePilot Angular application

This directory belongs at `scopepilot-ai/frontend`, next to `backend`.

See `../README-frontend.md` for setup, screenshots, and the current feature boundary. See `../docs/frontend-beginner-guide.md` for complete source code with exact destination paths.

**PowerShell — run inside `scopepilot-ai/frontend`:**

```powershell
npm.cmd ci
npm.cmd start
```

Open http://127.0.0.1:4200 and choose **Sample workspace** to try it without the .NET process.
